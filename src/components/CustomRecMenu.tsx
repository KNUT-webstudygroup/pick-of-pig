import styled, { css } from "styled-components";

function CustomRecMenu() {
  return <CustomRecMenuStyled></CustomRecMenuStyled>;
}

export default CustomRecMenu;

const CustomRecMenuStyled = styled.div`
  height: 100px;
  background-color: #000;
`;

const CustomMenus = styled.p``;
